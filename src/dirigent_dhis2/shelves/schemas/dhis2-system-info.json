{
  "$id": "dhis2-system-info",
  "title": "DHIS2 system info",
  "description": "The shape a GET /api/system/info returns: a top-level object, not a list, carrying at least the instance version and the server date. A schema that validates an object rather than an array.",
  "type": "object",
  "required": ["version", "serverDate"],
  "properties": {
    "version": { "type": "string" },
    "serverDate": { "type": "string", "format": "date-time" }
  }
}
